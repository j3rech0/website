---
import Nav from "./Nav.astro";
---

<header>
  <Nav />
</header>

<style lang="scss">
  @import "../styles/mixin.scss";
  header {
    max-width: var(--w-full);
    width: 100%;
    margin-inline: auto;
    height: fit-content;
    position: sticky;
    background: rgba(var(--c-black-gradient-1), 95%);
    top: 0;
    z-index: 2;
    box-shadow: 0px 20px 20px -10px rgba(var(--c-black-gradient-1), 1);
  }
</style>
